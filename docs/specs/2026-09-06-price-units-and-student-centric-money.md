# Единицы цены и перенос денег с курса на ученика

**Дата:** 2026-09-06
**Статус:** фазы 0, 1 и 1.5 в main (фаза 1.5 — 2026-09-14, миграция 038 накатана до деплоя); фазы 2–4 к реализации
**Область:** `migrations/`, `models/`, `repository/`, `service/`, `handlers/`, `router/`, `frontend/src/app/(dashboard)/{students,courses,payments,dashboard}`, `frontend/src/components/{students,courses,payments,lessons}`, `frontend/src/lib/{api,hooks}`, `frontend/src/schemas`, `frontend/src/types/api.ts`

**Предшествует:** `docs/specs/2026-08-31-calendar-events-and-scheduling-flow.md` (закрыта). Та спека сделала курс производной сущностью и завела раздел «Ученики». Эта — доводит начатое до денег: там, где навигация уже ученикоцентричная, модель данных всё ещё курсоцентричная.

**Делать после:** `docs/specs/2026-09-06-recurrence-series-bugs.md`. Статус той спеки сверен 2026-09-13: баги 1–4 исправлены и смержены (`feat/recurrence-retime`, `9a180ec`; архивация курса — `c884198`, `3a5a2de`), баг 5 откачен (`57f8e2c`), шапка файла это отражает. **Предусловие фазы 2 выполнено.** Порядок при этом важен: заморозка (п. 6.9) правит те же правила повторений, и делать её поверх незакрытого бага 2 было нельзя.

**Пересечения по файлам с той спекой** — знать до начала, чтобы не переписывать одно поверх другого:

| Файл | Там | Здесь |
|---|---|---|
| `repository/lesson.go` — `GetByCourse`, `GetByCoursePaged`, `GetByPeriod` | добавляются `rule_id`, `occurrence_date` | фаза 2 меняет расчёт позиции в цикле |
| `service/lesson.go` — `enrichLessons`, `computeCyclePositions` | не трогаются | фаза 2 добавляет `studentID` |
| `service/course.go` — `Delete` | архивация закрывает правила и сносит будущие уроки | не трогается |
| `router/router.go` | `courseService` создаётся после `lessonService` | фаза 2 добавляет `enrollmentRepo` в `paymentService` |
| `service/recurrence.go` — `Retime` (`UpdateTiming` + `SetMaterializedUntil`) | здесь чинился баг 2 (`scope=all` не пересоздаёт вхождения) | фаза 2 сдвигает `ends_on`/`max_count` при заморозке (п. 6.9) — **делать строго после** |

> Спека разбита на фазы. **Реализуй по одной фазе за раз**, каждая фаза самодостаточна и деплоится отдельно. Не начинай следующую, пока предыдущая не смержена.
>
> Фазы 0 и 1 чинят деньги и обязательны. Фаза 1.5 закрывает потерю платежей при удалении ученика и идёт строго до фазы 2 (п. 5a.4). Фазы 2–4 меняют модель и продукт; фаза 2 — самая ценная из них.

---

## 1. Проблема

### 1.1. Цена хранится пакетом, а две формы спрашивали её за урок

В схеме `courses` хранит пару `price_per_cycle` + `lessons_per_cycle` — «N уроков за X ₸». Цена урока нигде не хранится и везде получается делением. Сама модель верна (п. 2): она без потерь хранит и цену урока, умноженную на N, и пакет, который на N не делится. Сломано было другое — две формы из трёх спрашивали цену **за урок** и писали введённое число в колонку пакета без конвертации.

**`frontend/src/components/students/StudentOnboardingDialog.tsx`** — модалка «Новый ученик», основной вход для нового ученика:

- строки 59–63: дефолт считается делением `courses[0].price_per_cycle / (courses[0].lessons_per_cycle || 1)`, то есть поле в единицах «за урок»;
- строка 194: подпись `₸ за урок` — пользователю обещана цена за урок;
- строка 84: `if (price !== '') data.price_per_cycle = price` — то же число уходит в колонку пакета, **`lessons_per_cycle` не отправляется вообще**;
- `service/onboarding.go:66` при пустом `LessonsPerCycle` подставляет `1`.

Численно результат пока совпадает с ценой за урок — но только потому, что цикл молча схлопнулся в один урок. Следствия:

1. **Обычный размер пакета тьютора теряется.** Второй вход для нового ученика — календарь (`SlotCreatePopover` → `useCreateSlotLesson` → `courseRepo.GetOrCreateIndividual`, `repository/course.go:78-84`) — наследует *самые частые* `price_per_cycle` и `lessons_per_cycle` тьютора. У тьютора с пакетом 8 два входа «новый ученик» дают курсы с разной экономикой: 8 уроков против 1. А пакетный тьютор («85 000 за 12») через модалку свой пакет не введёт вовсе — она его не спрашивает.
2. **Дефолты дрейфуют.** `GetOrCreateIndividual` берёт модальное (самое частое) значение по всем активным курсам. Каждый ученик, заведённый через модалку, тянет это значение к 1 — и через несколько учеников неявные курсы из календаря тоже начинают создаваться с циклом 1, унаследовав при этом цену, которая по смыслу «за урок».
3. **Это мина под будущую правку.** Спека 8.1 в примере JSON шлёт `price_per_cycle: 5000` **и** `lessons_per_cycle: 1`. Кто-то реализует пример буквально, поставив цикл 8, — и цена мгновенно врёт в 8 раз, без единой ошибки в логах.
4. **Календарь склеивает чужие пакеты.** `GetOrCreateIndividual` выбирает самую частую цену и самое частое число уроков двумя независимыми подзапросами, поэтому пара может не совпасть ни с одним курсом тьютора. Пример с прода — п. 3.3.

**`frontend/src/app/(dashboard)/students/[id]/page.tsx:118-166`** (`CoursePrice`) — вторая точка. Инлайн-правка подписана «₸ за цикл» и пишет введённое число прямо в `price_per_cycle`, сохраняя `lessons_per_cycle` курса. Для пакетной модели это корректно: правится ровно та величина, которая хранится. Не хватало одного — N в подписи. «40 000 ₸ за цикл» не говорит, за сколько уроков, и тьютор, думающий ценой урока, вводит 5 000 в поле пакета из восьми занятий: цена молча занижается в 8 раз. Первая редакция этой спеки (вслед за спекой 6.5) прочитала это как «поле должно быть за урок» и в фазе 0 переделала правку на цену урока × N — что испортило пакеты, не делящиеся нацело (п. 4.2).

### 1.2. Куда протекала цена, введённая не в той единице

- **`repository/payment.go:GetMonthlyExpected`** — прогноз «ожидается в этом месяце» это `SUM(cp.price_per_cycle)` по стартам неоплаченных циклов с шагом `lessons_per_cycle`. Формула верна для обоих способов продавать курс; искажают её курсы, цена которых введена не в той единице, — а это главная денежная метрика дашборда.
- **`frontend/src/components/payments/PaymentForm.tsx:59-62`** — префилл `lessons_count = floor(amount / pricePerLesson)`, где `pricePerLesson` приходит из `courses/[id]/page.tsx:575` как `price_per_cycle / lessons_per_cycle`. Тьютор вводит сумму, форма сама решает, сколько уроков он оплатил. При цене, занижённой в 8 раз, форма насчитает в 8 раз больше оплаченных уроков — и баланс ученика уедет в плюс, которого нет.

### 1.3. Асимметрия валидации создания и правки

`models/course.go`:

```go
CreateCourseRequest.PricePerCycle  `validate:"gte=0"`        // строка 47
UpdateCourseRequest.PricePerCycle  `validate:"required,gt=0"` // строка 55
```

Неявный курс из календаря создаётся с ценой `0` (у тьютора ещё нет курсов — `COALESCE(..., 0)`), но сохранить `0` через форму или инлайн-правку нельзя: `required,gt=0` даёт 400, пользователь видит «Не удалось обновить цену» и не понимает, за что. Бесплатный или пробный курс завести невозможно, хотя докблок над `CreateCourseRequest` прямо объявляет это нормальным случаем.

### 1.4. Деньги висят на курсе, а не на человеке

`payments(course_id, amount, lessons_count, paid_at)` — платёж не знает, кто заплатил. Отсюда:

- **Группа.** У группового курса `student_id IS NULL`, платежи всех участников валятся в один ledger. `GetBalance` возвращает `SUM(lessons_count) − COUNT(проведённых)` по курсу целиком: для группы из пяти человек это число не значит ничего ни для кого. Отметки посещаемости (`lesson_attendances`) в баланс не входят вовсе.
- **Прогноз для группы занижен кратно составу.** `GetMonthlyExpected` группирует по курсу и прибавляет `price_per_cycle` один раз за старт цикла. Группа из пяти человек по 5 000 ₸ даёт в прогнозе 5 000, а не 25 000. При этом в схеме нигде не сказано, цена группы — за одного участника или за всех; поля для этого нет.
- **Ученик с двумя предметами — это проблема формы, а не схемы.** Платит одной суммой за математику и физику; тьютор обязан открыть две формы и разделить сумму сам, в уме. Две строки в БД здесь **правильны**: деньги покрывают разные предметы по разным ценам, и одна строка потеряла бы информацию, которая у тьютора есть. Чинится интерфейсом (п. 6.8), схему трогать не надо. Записано отдельно, чтобы не пойти менять модель там, где нужен фронт.
- **Главного вопроса не задать.** «Кто мне должен» из этой схемы не отвечается: долг существует только в разрезе курса, а тьютор думает о людях. Это, а не расписание, — то, ради чего он вообще заводит учёт.

### 1.5. Карточка ученика — тупик

Навигация уже ученикоцентричная (`Sidebar.tsx:403-404` — «Ученики» выше «Курсов»; в `MobileBottomNav.tsx` «Курсов» нет вообще), а содержимое — нет. Что где живёт:

| Что нужно репетитору | Где лежит | Где показано |
|---|---|---|
| Кто это, контакты | `students` | карточка ученика |
| Когда занимаемся | `lessons.course_id` | календарь + страница курса |
| Сколько стоит | `courses.price_per_cycle` | строка на карточке ученика + страница курса |
| Оплатил / сколько осталось | `payments.course_id` | **только** страница курса |
| Что задано | `courses.homework` | **только** страница курса |
| Доска, материалы | `course_id` | **только** страница курса |

Карточка ученика (`students/[id]/page.tsx`) — это контакты плюс строки курсов. Всё, ради чего человека открывают перед уроком, лежит на экране служебной сущности, которую предыдущая спека сознательно убрала из основного сценария.

### 1.6. Удаление ученика стирает деньги

`DELETE /students/:id` удаляет ученика физически (`repository/student.go:111`), а все внешние ключи к нему — `ON DELETE CASCADE` (миграции 001, 002). Цепочка `students → courses → payments` стирает платежи его индивидуальных курсов: доход прошлых месяцев уменьшается задним числом, уроки и посещаемость исчезают вместе с ними. Сервис проверяет только владение (`service/student.go:63-68`), `confirm` на фронте о потере истории не предупреждает. Колонка `students.active` существует с миграции 001, но нигде не выставляется. Архив курса этой беды не знает: `is_active = false`, а списки платежей и доход по активности курса не фильтруют.

Уход из группы (`repository/enrollment.go:66`) — тоже `DELETE`. Деньги группы сейчас висят на курсе и не теряются, но пропадает сам факт участия, на который опирается фаза 2 (п. 5a.4).

Удаление при этом нужно: ученик, заведённый по ошибке, засоряет список, счётчики и выбор в календаре. Задача — отличить ошибку от человека с историей (фаза 1.5, п. 5a).

---

## 2. Продуктовый принцип

**Цена — это пакет: N уроков за X ₸. Цена урока — производная.** Решение фаундера, 2026-09-13. Репетиторы продают курс двумя способами, и оба живые: одни называют цену урока и умножают её на число занятий («5 000 за урок, 8 уроков — 40 000»), другие называют сумму за пакет, из которой цена урока получается некруглой («85 000 за 12»). На проде 2026-09-13 — 22 активных курса у 4 тьюторов, примерно пополам: 32 000/8, 48 000/12, 128 000/16 рядом с 35 000/8 = 4 375, 45 000/8 = 5 625, 25 000/8 = 3 125, 85 000/12 = 7 083,33, 80 000/12 = 6 666,67. Двое из четырёх тьюторов пользуются обоими способами, цикл из одного урока — всего у двух курсов. Это не тип тьютора, а способ продать конкретный курс.

Пара «N уроков за X ₸» хранит оба способа без потерь. Цена урока всегда вычисляется — для показа, долга и подстановки в форму — делением и округлением до тенге. Хранить наоборот, цену урока, значило бы терять пакет у тех, кто назначал сумму: `ROUND(85000/12, 2) × 12 = 84 999,96`, и тьютор видел бы цену 7 083,33, которую никогда не назначал и не может ввести так, чтобы пакет сошёлся.

**Деньги принадлежат человеку, а не курсу.** Курс — техническая связка «ученик + предмет», созданная неявно. Долг, баланс и оплата — про человека: он платит, он должен, ему напоминают. Пока платёж знает только курс, продукт не может ответить на главный вопрос своего пользователя.

Следствие для UI: **цена вводится парой «сумма ₸ за N уроков» везде, где она вводится**, с подсказкой «≈ X ₸ за урок». Ни одна форма не спрашивает цену урока, чтобы потом умножить её на N: умножение на клиенте теряет пакет, который на N не делится.

---

## 3. Архитектурные решения

### 3.1. Канон — пакет `price_per_cycle` + `lessons_per_cycle`

Схема не меняется. Первая редакция этой спеки предлагала миграцию, делавшую каноном `price_per_lesson` и переименовывавшую `lessons_per_cycle` в `lessons_per_payment`; довод был «каноническая колонка хранит производную величину». Он верен только для тьюторов, которые умножают цену урока. У пакетных всё наоборот: исходная величина — сумма, а цена урока из неё выводится. Миграция отменена 2026-09-13 по данным прода (п. 2).

Миграция 012 (`4b79315`, 2026-06-05), заменившая `price_per_lesson` на пакет, была правильной: она сделала каноном то, что тьютор вводит руками, и данные прода это подтвердили.

Настоящая болезнь — не схема, а формы, спрашивавшие цену не в той единице, в которой её хранят. Лечится на границе ввода (фаза 1), без миграции и без двухшагового деплоя.

### 3.2. Имя `lessons_per_cycle` остаётся

Переименование в `lessons_per_payment` снято вместе с миграцией цены. «Цикл» — это ровно пакет, который тьютор продаёт: размер пачки для префилла формы оплаты, шаг нарезки будущих циклов в прогнозе и знаменатель цены урока. Позиция урока в цикле (`computeCyclePositions`, `service/lesson.go:575`) при этом считается по фактическим `payments.lessons_count` — плановый пакет и фактические платежи законно расходятся (доплатил за 4 урока вместо 8), и это не повод заводить второе имя.

### 3.3. Дефолты нового курса — самая частая пара, одно правило на оба входа

Дефолты для нового курса сейчас считаются в двух местах и по-разному. `GetOrCreateIndividual` (календарь) берёт самую частую цену и самое частое число уроков **двумя независимыми подзапросами** — и склеивает сумму одного пакета с количеством другого. У тьютора `df020b5e…` все активные курсы с разными ценами, поэтому «самая частая» по `ORDER BY count(*) DESC, price_per_cycle` оказывается просто наименьшей, 1 ₸, а самое частое число уроков — 12: новый курс из календаря получил бы «1 ₸ за 12 уроков». Модалка онбординга берёт первый курс из списка и спрашивает цену за урок, а сервис подставляет цикл 1.

Правило одно: **дефолт — самая частая пара `(price_per_cycle, lessons_per_cycle)` среди активных курсов тьютора, ничья решается самым поздним `started_at`**. Пара берётся из одного курса, поэтому несуществующий пакет получиться не может; при ничьей побеждает последний заведённый курс — он точнее прочих отражает нынешний прайс. Дальше порядок детерминирован — по сумме, затем по числу уроков, по возрастанию, — чтобы SQL и клиент не расходились на полной ничьей.

Правило реализовано в двух местах, и это осознанно. `GetOrCreateIndividual` вычисляет пару одним `LATERAL`-подзапросом внутри `INSERT … SELECT` — одна поездка в базу и защита от гонки через `ON CONFLICT` сохраняются. Модалка применяет то же правило на клиенте к уже загруженному списку курсов: ей дефолт нужен до сабмита, чтобы показать тьютору значения в полях. Отдельный метод `DefaultsForTutor` на бэкенде, как предлагала первая редакция, поэтому не нужен. **Клиентское правило зеркалит SQL — правишь одно, правь и другое.**

ponytail: модалка считает по `useCourses`, а он отдаёт первые 100 активных курсов (`coursesApi.list`). Для десятков курсов тьютора это весь список; клиент и SQL разойдутся, когда курсов станет больше сотни, — тогда отдавать дефолт ручкой.

### 3.4. `payments.student_id` — nullable в схеме, обязательный в API

Колонка добавляется как `NULL`-разрешённая: групповые платежи прошлого адресата не имеют и восстановить его неоткуда. Новые платежи обязаны нести `student_id` — это требование уровня валидации (`validate:"required,uuid"`), не схемы.

`NOT NULL` не ставим и «фиктивного» адресата легаси-строкам не выдумываем: неизвестное должно оставаться неизвестным, иначе баланс группы станет уверенно неправильным вместо честно пустого. Строки с `student_id IS NULL` показываются в списке платежей курса как «платёж группы (без адресата)» и в баланс конкретного ученика не входят.

### 3.5. Цена группы — за одного участника

После 3.4 двусмысленность из п. 1.4 снимается явно: пакет группового курса — цена **за одного участника**, `price_per_cycle` платит каждый. Прогноз для группы = цена пакета × число записанных учеников. Другого разумного варианта нет: платёж теперь адресный, а «цена за всех сразу» не делится на участников, когда один из них уходит.

### 3.6. Что сгорает из оплаченного — решено

**Сгорает любой состоявшийся урок: `status IN ('completed', 'missed')`. Посещаемость на деньги не влияет вообще.** Решение фаундера, 2026-09-06.

Логика: слот был забронирован под ученика и не был перепродан — значит он оплачен, пришёл ученик или нет. `lesson_attendances` остаётся чисто статистикой (кто как ходит), из денежного контура исключается полностью.

Побочные следствия, важные при реализации:

- Никаких `EXISTS`-подзапросов к `lesson_attendances` в денежных запросах. Баланс и прогноз считаются только по `lessons.status` и `payments`. Это заметно проще и быстрее, чем вариант с посещаемостью.
- Отменённый урок (`status = 'cancelled'`) не сгорает — тумбстоуны отменённых вхождений серии не должны съедать оплату.
- «Простить» пропуск тьютор может, отменив урок задним числом. Отдельной кнопки не заводим: смена статуса на `cancelled` — уже существующее действие с ровно этим смыслом.

> Правило «поздняя отмена всё равно списывает» (за N часов до начала) остаётся вне этой спеки: требует хранить время отмены и политику на уровне тьютора. См. раздел 9.

### 3.7. Дата записи в группу — `course_enrollments.enrolled_at`

Прямое следствие 3.6, без которого баланс группы будет неверным с первого дня.

Раз сгорает каждый состоявшийся урок курса, а не только посещённый, то ученик, записанный в группу в ноябре, немедленно «задолжает» за все занятия с сентября: строка в `course_enrollments` не помнит, когда он появился.

Поэтому в миграции 039 заводится `course_enrollments.enrolled_at`, и в счёт сгоревшего идут только уроки с `scheduled_at >= enrolled_at`. Для индивидуального курса записи в `course_enrollments` нет и вопрос не стоит: курс заводится вместе с учеником, все его уроки — его.

Бэкфилл существующих строк — **дата первой отметки посещаемости**, `courses.started_at` запасным вариантом. Дата первого платежа доуточняет её отдельным шагом уже после деплоя. Решение фаундера, 2026-09-09; способ уточнён 2026-09-09 после сверки с данными.

Один `started_at` для всех не годится, и это не мелочь: он говорит, когда началась группа, а не когда в ней появился человек. Присвоив его всем строкам, миграция создаст ровно тот долг с сентября, ради предотвращения которого колонка и заводится, — только не для будущих участников, а для всех нынешних сразу, в день деплоя.

**Почему не первый платёж, хотя он точнее.** Строки `course_enrollments` существуют только у групповых курсов — `enrollmentService.Add` отказывает индивидуальным с `ErrForbidden`. А групповым платежам миграция 039 адресата не выдаёт принципиально (п. 3.4: восстановить его неоткуда). Значит в момент накатки `payments.student_id` у всех групповых платежей — `NULL`, и подзапрос «первый платёж этого ученика по этому курсу» вернёт пусто для **каждой** строки без исключения. Это была бы не неточность, а мёртвая ветка, молча вырождающаяся в тот самый `started_at`.

`lesson_attendances` — единственный след участия, который у групп есть уже сейчас: отметку ставят на конкретного человека на конкретном уроке (п. 3.6 выводит эту таблицу из денежного контура, но как свидетельство присутствия она остаётся). Она неточна в другую сторону — если тьютор начал отмечать посещаемость позже, чем человек пришёл, дата окажется поздней, и ученик недоплатит за пару занятий. Ошибка в пользу ученика здесь предпочтительнее: несуществующий долг разрушает доверие к цифрам, недостача в два урока — нет.

**Посещаемость в группах действительно ведётся** — подтверждено фаундером 2026-09-10. То есть основной источник даты рабочий, и `started_at` останется редким запасным случаем, а не общим правилом.

Платежи возвращаются в игру **после** того, как тьютор вручную проставит адресатов легаси-платежам группы (п. 6.1): тогда одноразовый уточняющий `UPDATE` заменит `started_at` на дату первого платежа там, где посещаемости не было вовсе.

### 3.8. Цикл-бейдж — только там, где известен ученик

`CycleBadge` («3 из 8») на групповом уроке в календаре смысла не имеет: у урока нет одного ученика, а после фазы 2 позиция в цикле считается по платежам конкретного человека. Решение: в календаре и на странице курса бейдж показывается только для индивидуальных курсов; в карточке ученика — всегда, там ученик известен.

Это упрощает `enrichLessons` и `GetCalendar`: для групп цикл просто не считается, вместо того чтобы считаться неправильно.

### 3.9. Периоды участия — одна ось для записи, ухода и заморозки

`enrolled_at` (с какого числа уроки считаются его), `left_at` (по какое) и заморозка (какие куски вырезаны из середины) — три ответа на один вопрос: **в какие периоды человек считается занимающимся**. Сделанные по отдельности, они дают три механизма, которые придётся согласовывать в каждом из четырёх денежных мест — баланс, долги, прогноз, позиция в цикле. Сделанные как интервалы — это два `COALESCE` и один `NOT EXISTS` внутри выражения `burned`, которое п. 6.4 и так велит собрать один раз и переиспользовать.

Отсюда три решения фазы 2 (все — решения фаундера, 2026-09-09):

- **Уход из группы мягкий.** `course_enrollments.left_at TIMESTAMPTZ NULL`, `enrollmentRepo.Remove` (`repository/enrollment.go:66`) из `DELETE` превращается в `UPDATE`. Пока строка была чистой связкой, физическое удаление было правильным; с датой внутри она стала куском финансовой истории, и удаление превратилось в потерю данных: ученик, вышедший с долгом, исчезал бы из `/payments/debts` вместе с долгом, а его платежи оставались бы строками без пары. Дата, а не флаг `is_active`: границу «по какое число уроки сгорают» всё равно надо знать, а `left_at IS NULL` читается как «занимается» не хуже булева поля и не требует второй колонки. **Перенесено в фазу 1.5** (п. 5a.4, 2026-09-13): запись, удалённая до фазы 2 нынешним `DELETE`, выпала бы из ручной разметки легаси-платежей группы.
- **Заморозка — интервал на ученике, а не на курсе.** `student_pauses(student_id, starts_on, ends_on)` без `course_id`: «уехал на месяц» — свойство человека, и одно действие закрывает все его предметы и группы сразу. Пара «ученик + предмет» отдельно не морозится: случай редкий, а поле выбора предмета платили бы все.
- **Пауза вырезает уроки по датам, а не по статусам.** Поэтому заморозка задним числом («он же болел всю прошлую неделю») работает тем же действием и тем же кодом, что и заморозка вперёд.

---

## 4. Фаза 0 — единицы цены, hotfix без миграций

**Цель:** убрать обе известные ошибки единиц и снять мину, не трогая схему. Деплоится сегодня, откатывается revert'ом.

**Файлы:** `models/course.go`, `frontend/src/components/students/StudentOnboardingDialog.tsx`, `frontend/src/app/(dashboard)/students/[id]/page.tsx`, `frontend/src/types/api.ts`.

### 4.1. Модалка онбординга шлёт цикл явно

`StudentOnboardingDialog.tsx`, строка 84:

```ts
// Было:
if (price !== '') data.price_per_cycle = price

// Стало: поле спрашивает цену за урок — значит цикл равен одному уроку,
// и это должно быть сказано вслух, а не оставлено на дефолт сервера.
if (price !== '') {
  data.price_per_cycle   = price
  data.lessons_per_cycle = 1
}
```

Поведение не меняется ни на йоту — меняется то, что инвариант «поле в единицах за урок ⇒ цикл = 1» теперь записан в коде, а не держится на молчаливом дефолте `service/onboarding.go:66`. Комментарий на строках 59–61 расширить: сказать, что обратное умножение не нужно именно потому, что цикл единичный.

> **Заменено в фазе 1 (п. 5.2).** Модалка спрашивает пару «сумма ₸ за N уроков», и единичный цикл больше не навязывается: он был верен, только пока поле спрашивало цену урока.

### 4.2. Инлайн-правка цены на карточке ученика — в единицах «за урок»

`students/[id]/page.tsx`, компонент `CoursePrice` (строки 118–166). Это исправление реальной ошибки в 8 раз, а не косметика.

- Показывать `Math.round(course.price_per_cycle / (course.lessons_per_cycle || 1))` с подписью `₸ / урок`.
- В `draft` класть то же вычисленное значение, а не `course.price_per_cycle`.
- В `save()` писать обратно с умножением:

```ts
const perLesson = Number(draft)
if (!Number.isFinite(perLesson) || perLesson < 0) return
const nextCycle = perLesson * (course.lessons_per_cycle || 1)
if (nextCycle === course.price_per_cycle) return
await update.mutateAsync({
  subject:           course.subject,
  price_per_cycle:   nextCycle,
  lessons_per_cycle: course.lessons_per_cycle,
  started_at:        course.started_at,
  ended_at:          course.ended_at ?? undefined,
})
```

- Строку `{course.lessons_per_cycle} в цикле` (строка 95) оставить как есть — она честно описывает размер пачки.

> **Заменено в фазе 1 (п. 5.1), 2026-09-13.** Эта версия исходила из посылки «цена всегда за урок» и на пакетах, которые на N не делятся, портит данные. Правка сохраняет на потере фокуса (`onBlur={save}`), а `draft` — округлённая цена урока, поэтому достаточно кликнуть по цене и кликнуть мимо: 85 000/12 → в поле 7 083 → в БД 84 996; 80 000/12 → 80 004; 1/12 → 0. Проверка `nextCycle === course.price_per_cycle` не спасает: после округления туда и обратно числа уже не равны. Версия попала в `origin/main` вместе с фазой 0; на момент обнаружения значения на проде были целы.

### 4.3. Снять асимметрию валидации

`models/course.go:55`:

```go
PricePerCycle float64 `json:"price_per_cycle" validate:"gte=0"`  // было required,gt=0
```

`required` с числовым полем в go-playground/validator означает «не ноль», поэтому его снятие и есть починка: без него нулевая цена сохраняется, а `gte=0` продолжает ловить отрицательную. Тот же `gte=0` уже стоит в `CreateCourseRequest` — теперь создание и правка ведут себя одинаково.

Фронтовая проверка `price < 0` в `CoursePrice` и `min(0)` в `schemas/course.ts` уже согласованы с этим, менять их не надо.

### 4.4. Форма оплаты: вводятся уроки, сумма выводится

Третья тихая потеря того же семейства. `PaymentForm.tsx:59-62`:

```ts
useEffect(() => {
  if (!isEdit && pricePerLesson > 0 && amount > 0) {
    setValue('lessons_count', Math.floor(amount / pricePerLesson))
  }
}, [amount, pricePerLesson, setValue, isEdit])
```

Тьютор вводит сумму, форма делит её на цену и **округляет вниз**. 12 000 при цене 5 000 записываются как 2 оплаченных урока: 2 000 ₸ исчезают из балансового учёта молча. Деньги при этом сохраняются верно (`amount = 12000`), расходится именно `lessons_count` — то есть баланс, долг и позиция в цикле.

**Направление вывода перевернуть: вводятся уроки, сумма подставляется.**

Обоснование — асимметрия ущерба. В `payments` живут две независимые величины, и питают они разное: `lessons_count` → баланс, цикл, «сколько должен» (вся суть продукта); `amount` → только доход за месяц. Ошибка в первом тихо портит то, ради чего продукт заведён, и всплывает через месяц при сверке с родителем; ошибка во втором портит строку отчёта, которую тьютор и так сверяет с банком. Терять надо там, где дешевле — сейчас теряется там, где дороже.

Плюс `amount = уроки × цена` **не инвариант**: скидка, округление, «докинули сверху», доплата частями, изменившаяся цена. Формула, жёстко сцепляющая поля, соврёт в любом случае — вопрос только в какую сторону.

Что сделать:

1. Поля местами: «Уроков оплачено» — первое, «Сумма» — второе.
2. Автоподстановка **строго односторонняя**: `lessons → amount`, и только пока тьютор не тронул сумму руками. Флаг «поле трогали», не двусторонняя привязка — иначе поля начнут переписывать друг друга по мере набора (нынешний `useEffect` имеет ровно такую форму).
3. Предзаполнение при открытии — `lessons_per_cycle` курса: обычный пакет этого тьютора. В типичном «оплатил как всегда» форма готова к сохранению сразу.
4. Расхождение показывать, но **не блокировать**: подпись вида «выходит 4 500 ₸ за урок вместо 5 000», когда числа разошлись. Скидки и округления законны; форма, не дающая сохранить, пока не сойдётся до тенге, будет обходиться враньём в полях.

То же правило действует в форме разбивки по предметам (п. 6.8) — одно правило на обе формы.

**Проверить исторические данные.** Платежи до починки могли недосчитать уроки. Запрос ищет платежи, где сумма и число уроков не сходятся по текущей цене курса. Сравнение — в целых тенге: цена урока здесь частное (85 000 / 12 = 7 083,33…), и точное `<>` ловит погрешность деления вместо расхождений.

```sql
WITH priced AS (
  SELECT p.id, p.paid_at, p.amount, p.lessons_count, c.subject,
         COALESCE(NULLIF(btrim(s.first_name || ' ' || s.last_name), ''), '— группа —') AS student,
         c.price_per_cycle / NULLIF(c.lessons_per_cycle, 0) AS per_lesson
    FROM payments p
    JOIN courses  c ON c.id = p.course_id
    LEFT JOIN students s ON s.id = c.student_id
)
SELECT paid_at::date, student, subject,
       round(per_lesson)                          AS per_lesson,
       amount, lessons_count,
       round(amount / per_lesson, 2)              AS implied_lessons,
       round(amount - lessons_count * per_lesson) AS unaccounted,
       id
  FROM priced
 WHERE per_lesson > 0
   AND abs(amount - lessons_count * per_lesson) >= 1
 ORDER BY abs(amount - lessons_count * per_lesson) DESC;
```

**Автоматически не править:** запрос сравнивает с *текущей* ценой курса, а истории цен нет (п. 9), поэтому строка может означать и недосчитанный урок, и законную скидку, и то, что цену сменили уже после платежа. Это список кандидатов, а не приговор; решает фаундер.

**Результат на проде, 2026-09-13:** два кандидата, следов округления вниз нет ни одного.

- `Жангир, SAT, 2026-07-15` — 65 000 за 8 уроков при нынешних 85 000 за 12. Похоже, пакет сменили после платежа: второй платёж (2026-08-11) уже ровно по новой цене. Для своего времени строка верна.
- `Жанна, English, 2026-05-31` — 18 000 за 3 урока при цене 4 500. Округлять тут было нечего — 18 000 делится на 4 500 нацело, так что 3 введено руками; остальные четыре её платежа идут ровно по 4 500 за урок. Либо часть суммы закрывала прошлый долг, либо у ученицы в балансе не хватает урока. Решение — за фаундером.

**Критерии приёмки**

- Тьютор с курсами по 40 000 ₸ / 8 уроков заводит ученика через модалку с ценой 5 000 → в БД `price_per_cycle = 5000`, `lessons_per_cycle = 1`; на карточке ученика строка читается «5 000 ₸ / урок · 1 в цикле». Модалку переделывает фаза 1 (п. 5.2).
- На курсе с `lessons_per_cycle = 8` и `price_per_cycle = 40000` инлайн-правка показывает `5 000`; ввод `6 000` даёт в БД `price_per_cycle = 48000`, а не `6000`. Правку переделывает фаза 1 (п. 5.1): на пакетах, не делящихся на N, эта версия портила цену.
- Цену курса можно сохранить равной нулю: инлайн-правка и `CourseForm` возвращают 200, а не 400.
- Оплата на 12 000 при цене 5 000: тьютор вводит 2 урока → сумма подставляется 10 000, он правит её на 12 000, форма показывает «6 000 ₸ за урок вместо 5 000» и сохраняет `lessons_count = 2`, `amount = 12000`. Ни одно число не меняется само после ручной правки.
- `go test ./...` зелёный; тесты, проверявшие `gt=0` на `UpdateCourseRequest`, обновлены.

---

## 5. Фаза 1 — формы цены под пакетную модель

**Цель:** цену вводят и правят в той единице, в которой она хранится, — парой «сумма ₸ за N уроков»; оба входа «новый ученик» предлагают один и тот же пакет. Миграций нет, деплой обычный. Решение фаундера, 2026-09-13; работа — в ветке `fix/package-pricing`.

**Файлы:** `frontend/src/app/(dashboard)/students/[id]/page.tsx`, `frontend/src/components/students/StudentOnboardingDialog.tsx`, `repository/course.go`.

**Не меняются:** `CourseForm` — уже спрашивает цену за цикл и число уроков; форма оплаты — вводятся уроки, сумма подставляется в целых тенге, за полный пакет выходит ровно его сумма; прогноз `GetMonthlyExpected` — суммирует цены пакетов по стартам неоплаченных циклов, что верно для обоих способов; схема и API.

### 5.1. (а) Инлайн-правка цены — сумма пакета

`CoursePrice` в `students/[id]/page.tsx` правит `price_per_cycle` как есть, без округления туда и обратно:

- показ: кликабельная сумма «85 000 ₸», рядом — «за 12 ур. · ≈ 7 083 / урок»; при `lessons_per_cycle = 1` — «6 000 ₸ за урок». Эта подпись заменяет прежнюю строку «`{lessons_per_cycle} в цикле`»;
- `draft` — `price_per_cycle`; `save()` пишет введённое число в `price_per_cycle` и сохраняет `lessons_per_cycle` курса;
- неизменённое значение не сохраняется: введённое сравнивается с исходной суммой пакета, а не с результатом умножения, поэтому клик и потеря фокуса ничего не пишут;
- пустое поле при потере фокуса — «передумал», а не «цена 0»: `Number('')` даёт 0 и молча обнулил бы прайс.

### 5.2. (б) Модалка «Новый ученик» — пара полей

Вместо «[ цена ] ₸ за урок» — «[ сумма ] ₸ за [ N ] уроков», как в `CourseForm`, с подсказкой «≈ X ₸ за урок» при N > 1. Отправляются оба поля, `price_per_cycle` и `lessons_per_cycle`. Явный `lessons_per_cycle: 1` из п. 4.1 уходит.

Дефолты — по правилу п. 3.3: самая частая пара среди активных курсов тьютора, ничья → самый поздний `started_at`. Прежняя модалка брала `courses[0]`, то есть первый курс в порядке ответа API, и к прайсу тьютора это отношения не имело.

Число уроков — целое от 1, иначе форма не отправляется. Проверка нужна именно на клиенте: очищенное поле даёт `Number('') = 0`, бэкенд пропускает 0 через `omitempty`, а сервис онбординга подставляет 1 — пакет «80 000 за 12» молча превратился бы в «80 000 за 1 урок».

Подстановка `1` при пустом `LessonsPerCycle` в `service/onboarding.go:66` остаётся страховкой для клиента, который цикл не прислал; модалка присылает его всегда, когда присылает цену.

### 5.3. (в) `GetOrCreateIndividual` — пара одним подзапросом

`repository/course.go`, вставка неявного курса из календаря. Два независимых скалярных подзапроса заменяются одним `LATERAL`, который берёт пару из одного курса:

```sql
INSERT INTO courses (student_id, tutor_id, subject, price_per_cycle, lessons_per_cycle, started_at)
SELECT s.id, $1::uuid, $3::text,
       COALESCE(d.price_per_cycle, 0),
       COALESCE(d.lessons_per_cycle, 1),
       $4::timestamptz
  FROM students s
  LEFT JOIN LATERAL (
       SELECT price_per_cycle, lessons_per_cycle
         FROM courses
        WHERE tutor_id = $1::uuid AND is_active
        GROUP BY price_per_cycle, lessons_per_cycle
        ORDER BY count(*) DESC, max(started_at) DESC, price_per_cycle, lessons_per_cycle
        LIMIT 1) d ON TRUE
 WHERE s.id = $2::uuid AND s.tutor_id = $1::uuid
ON CONFLICT DO NOTHING
RETURNING …
```

`LEFT JOIN … ON TRUE` сохраняет поведение для первого курса тьютора: пары нет, `COALESCE` даёт 0 ₸ за 1 урок, как и раньше. Гонка по-прежнему закрыта `ON CONFLICT` — вставка остаётся одним запросом.

### 5.4. Проверочный запрос — как тьюторы задают цены

До мержа и после деплоя — чтобы видеть, что оба способа живы и какой пакет получит дефолтом каждый тьютор:

```sql
SELECT c.tutor_id,
       c.lessons_per_cycle AS n,
       c.price_per_cycle   AS package,
       round(c.price_per_cycle / c.lessons_per_cycle, 2) AS per_lesson,
       CASE WHEN c.lessons_per_cycle = 1                     THEN 'цена за урок'
            WHEN c.price_per_cycle % c.lessons_per_cycle = 0 THEN 'делится нацело'
            ELSE 'пакет, не делится' END AS kind,
       count(*) AS courses
  FROM courses c
 WHERE c.is_active
 GROUP BY 1, 2, 3, 4, 5
 ORDER BY 1, 2, 3;
```

«Делится нацело» не доказывает умножение — 90 000/12 = 7 500 подходит под оба способа; «пакет, не делится» доказывает сумму за пакет. Результат на 2026-09-13 — в п. 2.

**Критерии приёмки**

- Пакет 85 000 / 12: клик по цене на карточке ученика и потеря фокуса без ввода — в БД по-прежнему `price_per_cycle = 85000`. Ввод 90 000 → `price_per_cycle = 90000`, `lessons_per_cycle = 12`.
- Курс 6 000 / 1 на карточке читается «6 000 ₸ за урок»; курс 85 000 / 12 — «85 000 ₸ за 12 ур. · ≈ 7 083 / урок».
- Модалка и календарь у тьютора `df020b5e…` предлагают 24 000 за 4, а не 1 ₸ за 12. Проверено запросом на проде 2026-09-13: курсы 24 000 / 4 и 80 000 / 12 начаты в один день, и ничья по частоте и по дате решается меньшей суммой. У трёх остальных тьюторов пакет по умолчанию не изменился — правка задела только сломанный случай.
- Новый курс из календаря у того же тьютора получает ту же пару, что предлагает модалка.
- Ученик, заведённый через модалку у тьютора с пакетом 8, получает курс с `lessons_per_cycle = 8`, а не 1.
- Очищенное поле «N уроков» в модалке останавливает форму с ошибкой, а не создаёт пакет «за 1 урок».
- Первый курс тьютора, у которого других курсов нет: модалка и календарь дают 0 ₸ за 1 урок, как раньше.
- `repository/course_integration_test.go` проходит на новом SQL и падает на старом ровно двумя прежними багами: 1 ₸ вместо текущего пакета и число уроков из чужого пакета. Проверено откатом SQL 2026-09-13.
- `go test ./...` и `make test-integration` зелёные, `eslint` по затронутым файлам чистый. Исключения, существовавшие до этой фазы: `TestGetAllByTutor_CarriesSubjectAndStudentName` красный с 2026-08-24 — вставляет `NULL` в `students.last_name`, который миграция 030 сделала `NOT NULL`; `npm run lint` по всему проекту — 22 ошибки, в основном `react-hooks` в синхронизации доски.

---

## 5a. Фаза 1.5 — удаление ученика без потери истории

**Цель:** ученик, заведённый по ошибке, удаляется бесследно; ученик с историей уходит в архив, и его платежи, уроки и посещаемость остаются. Уход из группы перестаёт стирать запись об участии. Решение фаундера, 2026-09-13; ветка `feat/student-archive`.

**Миграция:** `038_enrollments_left_at.sql` — одна nullable-колонка. Фаза 2 из-за этого получает номер 039.

**Файлы:** `migrations/038_enrollments_left_at.sql`, `repository/{student,enrollment,course}.go`, `service/{student,enrollment}.go`, `handlers/student.go`, `router/router.go`, `frontend/src/app/(dashboard)/students/{page.tsx,[id]/page.tsx}`, `frontend/src/lib/{api/students.ts,hooks/useStudents.ts}`, `frontend/src/types/api.ts`.

### 5a.1. Критерий «у ученика есть история»

| След | Откуда |
|---|---|
| платёж по его индивидуальному курсу, в том числе архивному | `payments` → `courses.student_id` |
| проведённый или пропущенный урок его индивидуального курса | `lessons.status IN ('completed', 'missed')` |
| отметка посещаемости в группе | `lesson_attendances.student_id` |

Историей не считаются запланированные и отменённые уроки, запись в группу без отметок, архивный курс без платежей и проведённых уроков — всё это уходит вместе с учеником, не искажая ни денег, ни статистики. Групповые платежи в критерий сейчас не входят: адресата у них нет (п. 3.4). Фаза 2 дополняет критерий адресными платежами (п. 6.3).

Проведённый урок — история даже без единого платежа: после фазы 2 это долг (п. 3.6), и удаление стёрло бы должника вместе с долгом. Цена решения: ученик, заведённый по ошибке и замеченный через неделю, уже имеет `completed`-уроки — их проставила фоновая джоба автозавершения из `main.go`. Выход — отменить эти уроки и удалить ученика: отмена задним числом — существующее действие ровно с этим смыслом (п. 3.6).

### 5a.2. Удаление — только без истории

`repository/student.go:Delete` проверяет и удаляет одним запросом и возвращает число удалённых строк:

```sql
DELETE FROM students s
 WHERE s.id = $1 AND s.tutor_id = $2
   AND NOT EXISTS (SELECT 1 FROM payments p JOIN courses c ON c.id = p.course_id
                    WHERE c.student_id = s.id)
   AND NOT EXISTS (SELECT 1 FROM lessons l JOIN courses c ON c.id = l.course_id
                    WHERE c.student_id = s.id AND l.status IN ('completed', 'missed'))
   AND NOT EXISTS (SELECT 1 FROM lesson_attendances la WHERE la.student_id = s.id)
```

Отдельный `SELECT` перед `DELETE` оставил бы окно, в котором только что записанный платёж уехал бы в каскад; в одном запросе это окно сужается до длительности одного оператора, а не исчезает: на READ COMMITTED `DELETE` берёт свой снепшот, и платёж из конкурентной транзакции, закоммиченной уже после его старта, каскад всё равно заберёт.

`studentService.Delete` уже находит ученика через `GetByID` (`service/student.go:63-68`). Нашёл, а `DELETE` затронул 0 строк — у ученика есть история: `ErrConflict` → **409**. Второй вызывающий `studentRepo.Delete` — откат онбординга (`service/onboarding.go:128`) — не затронут: у только что созданного ученика история появиться не успевает.

Фронт — `handleDelete` в `students/page.tsx` и `students/[id]/page.tsx`:

1. `confirm("Удалить X?")` → `DELETE /students/:id`; 204 — как сейчас.
2. 409 → второй `confirm`: «У X есть платежи или проведённые уроки — удаление стёрло бы их из истории. Перенести в архив?» → `POST /students/:id/archive`.

Флаг «есть история» в ответ `/students` не добавляем: 409 сообщает то же самое ровно тогда, когда это нужно, без трёх `EXISTS` на каждую строку списка.

### 5a.3. Архив ученика

Колонка `students.active` есть с миграции 001 и до сих пор нигде не выставляется — схема для архива не меняется.

**`POST /students/:id/archive`** — `studentService.Archive`, шаги строго в этом порядке:

1. Каждый активный индивидуальный курс ученика — через `courseService.Delete` (`service/course.go:85`): `is_active = false`, правила закрываются, будущие уроки удаляются; прошедшие уроки и платежи остаются.
2. Записи в группы — `left_at = NOW()` (п. 5a.4).
3. Refresh-токены кабинета — существующим `studentRefreshTokenRepo.DeleteByStudentID`.
4. `students.active = false`.

Транзакции нет, и она не нужна: каждый шаг идемпотентен, а `active = false` стоит последним и служит признаком «архивация завершена». Сбой посередине оставляет ученика в списке, повторный клик доделывает. Зависимости — узкими интерфейсами, как в `service/onboarding.go`.

**`POST /students/:id/restore`** — `active = true`, и только. Курсы остаются в архиве курсов и восстанавливаются там же (`POST /courses/:id/restore`); в группу ученика добавляют заново — повторная запись снимает `left_at` (п. 5a.4).

**Списки.** `GET /students` отдаёт только `active = true`, с `?archived=true` — только архивных. Через этот эндпоинт идут выбор ученика в календаре (`StudentCombobox`), выбор участников группы (`courses/[id]/page.tsx`), `courses/page.tsx` и счётчик на дашборде (`useStudentCount`) — все чинятся одним условием. На `/students` — переключатель «Архив» и «Восстановить» в строке; тип `Student` во фронте получает `active`.

**Карточка архивного ученика** открывается по прямой ссылке (`GetByID` активность не проверяет) с плашкой «В архиве» и кнопкой «Восстановить». Курсов на ней нет: `GetByStudent` показывает только активные.

**Кабинет.** `GetCredentialsByLogin` и `GetByInviteToken` (`repository/student.go`) получают `AND active`: архивный ученик не входит и не активирует приглашение; refresh отозван шагом 3.

ponytail: access-JWT ученика живёт 30 дней (`handlers/student_auth.go:164`), а `middleware.AuthStudent` в базу не ходит — уже открытая сессия архивного ученика до истечения токена видит в кабинете свою историю. Данные при этом его собственные, будущих уроков у архивированных курсов нет, к доске групп не пускает `EnrolledInLesson` (п. 5a.4). Апгрейд — проверка `active` в `AuthStudent`, если окно станет проблемой.

**Запись архивного в группу.** `enrollmentRepo.AddBulk` получает `AND s.active` в выборке из `students`, `enrollmentService.Add` архивному отказывает. Пикеры архивных и так не показывают — это на случай вкладки, открытой до архивации.

### 5a.4. Уход из группы — `left_at`

Переносится из фазы 2 (п. 3.9). Между этой фазой и фазой 2 запись об участии терять нельзя: фаза 2 опирается на неё дважды — `IsEnrolled` (п. 6.3) пропускает ручную разметку легаси-платежей группы только для записанных, а бэкфилл `enrolled_at` (п. 6.1) ищет посещаемость по строкам `course_enrollments`. Ученик, убранный из группы нынешним `DELETE`, выпал бы из обоих.

```sql
-- +goose Up
-- Уход из группы мягкий: строка записи — кусок истории участия. Фаза 2 строит
-- на ней разметку легаси-платежей и периоды участия (спека, п. 5a.4).
ALTER TABLE course_enrollments ADD COLUMN left_at TIMESTAMPTZ;  -- NULL = занимается

-- +goose Down
-- Прежний смысл «убран — строки нет»: иначе после отката ушедшие снова в группе.
DELETE FROM course_enrollments WHERE left_at IS NOT NULL;
ALTER TABLE course_enrollments DROP COLUMN left_at;
```

- `enrollmentRepo.Remove` — `UPDATE course_enrollments SET left_at = NOW() WHERE course_id = $1 AND student_id = $2 AND left_at IS NULL`.
- Текущий состав — `AND ce.left_at IS NULL` в трёх местах:
  - `enrollmentRepo.GetByCourse` — состав группы на странице курса;
  - `courseRepository.GetByStudent` — групповые курсы на карточке ученика;
  - `studentRepository.EnrolledInLesson` — доступ ученика к уроку и доске.
- `studentRepository.ListLessons` не показывает уроки группы после ухода: `l.scheduled_at < COALESCE(ce.left_at, 'infinity')` — та же граница, что в `burned` (п. 6.4). Прошлые уроки остаются: кабинет намеренно показывает всю историю, включая архивные курсы (комментарий в `ListLessons`). `ListCourses` и `ListHomework` по тому же правилу не меняются.
- `Add` и `AddBulk` — `ON CONFLICT (course_id, student_id) DO UPDATE SET left_at = NULL WHERE course_enrollments.left_at IS NOT NULL`: возвращение ушедшего снимает уход, запись уже состоящего ведёт себя как сейчас.

ponytail: повторная запись склеивает периоды участия в один. После фазы 2 уроки между уходом и возвращением сгорят: `enrolled_at` прежний, `left_at` снят. Ceiling — редкий случай «ушёл и вернулся в ту же группу»; апгрейд — таблица периодов, до неё промежуток закрывается заморозкой (п. 6.9).

### 5a.5. Что не меняется

- Списки платежей и доход месяца (`repository/payment.go:139-212`) не фильтруют ни по `courses.is_active`, ни по `students.active`: история архивного ученика видна, как была. Прогноз `GetMonthlyExpected` считает только активные курсы — архивный ученик перестаёт быть ожидаемым доходом сам.
- `ON DELETE CASCADE` на ключах к `students` остаётся: теперь он срабатывает только на учениках без истории (п. 5a.2) и каскадом от `tutors`.

**Критерии приёмки**

- Ученик, заведённый через модалку с расписанием, без платежей и проведённых уроков: «Удалить» → 204, ученик исчезает вместе с курсом и будущими уроками.
- Ученик с платежом: «Удалить» → 409 → «Перенести в архив?» → ученика нет в списке, он есть в архиве; платёж — в `/payments` и в доходе месяца; будущие уроки удалены, прошедшие — в календаре.
- Ученик без платежей, но с `completed`-уроком → 409; после отмены урока удаление проходит.
- Участник группы с отметкой посещаемости → 409.
- Архивного ученика нет в выборе ученика в календаре, в выборе участников группы и в счётчике дашборда; вход в кабинет и refresh отклонены.
- «Восстановить» возвращает ученика в список; его курсы остаются в архиве курсов.
- Убранный из группы пропадает из состава и с карточки ученика; строка `course_enrollments` на месте с заполненным `left_at`; в кабинете прошлые уроки группы видны, будущие — нет; на доску урока группы не пускает.
- Повторное добавление ушедшего в ту же группу снимает `left_at` без дубля строки; добавление уже состоящего — как раньше.
- Миграция 038 накатана, откатана и накатана снова на локальной БД.
- `make test-integration`: запрос удаления проверен на каждом из трёх следов истории и на ученике без них; `Remove` и повторный `AddBulk` — на реальной БД. `go test ./...` зелёный.

---

## 6. Фаза 2 — миграция 039: деньги на ученике

**Цель:** платёж знает, кто заплатил; баланс и долг считаются по человеку с учётом периодов его участия; появляется экран «Кто должен».

**Предусловие:** решение п. 3.6 принято — сгорает любой состоявшийся урок, посещаемость денег не касается. Заморозка (п. 6.9) дополнительно требует, чтобы был закрыт баг 2 спеки по сериям: она правит `ends_on`/`max_count` тех же правил.

### 6.1. Миграция `039_payments_student.sql`

```sql
-- +goose Up
-- Платёж адресный: долг и баланс — свойства человека, а не курса. Групповой
-- курс (student_id IS NULL) вообще не даёт понять, кто из пяти заплатил.
ALTER TABLE payments ADD COLUMN student_id UUID REFERENCES students(id) ON DELETE CASCADE;

-- Индивидуальные курсы бэкфиллятся однозначно: у курса ровно один ученик.
UPDATE payments p
   SET student_id = c.student_id
  FROM courses c
 WHERE c.id = p.course_id AND c.student_id IS NOT NULL;

-- Групповые платежи адресата не имеют и восстановить его неоткуда: остаются
-- NULL и в баланс конкретного ученика не входят (спека, п. 3.4).

CREATE INDEX idx_payments_student ON payments(student_id) WHERE student_id IS NOT NULL;

-- Периоды участия (спека, п. 3.9): с какого числа уроки ученика сгорают; по
-- какое — left_at, заведённый миграцией 038 (фаза 1.5). Без enrolled_at ученик,
-- добавленный в ноябре, задолжает за все занятия с сентября: сгорает каждый
-- состоявшийся урок курса, а строка не помнит, когда участник появился.
ALTER TABLE course_enrollments ADD COLUMN enrolled_at TIMESTAMPTZ;

-- Строки course_enrollments бывают только у групповых курсов, а групповым
-- платежам student_id здесь не выдаётся (см. выше) — значит искать «первый
-- платёж этого ученика» бессмысленно, подзапрос вернул бы NULL для всех строк.
-- Единственный след участия, который есть у групп сейчас, — посещаемость;
-- started_at остаётся для тех, кого ни разу не отмечали (спека, п. 3.7).
UPDATE course_enrollments ce
   SET enrolled_at = COALESCE(
         (SELECT min(l.scheduled_at)
            FROM lesson_attendances la
            JOIN lessons l ON l.id = la.lesson_id
           WHERE l.course_id = ce.course_id AND la.student_id = ce.student_id),
         c.started_at)
  FROM courses c
 WHERE c.id = ce.course_id;

ALTER TABLE course_enrollments
  ALTER COLUMN enrolled_at SET NOT NULL,
  ALTER COLUMN enrolled_at SET DEFAULT NOW();

-- Заморозка (спека, п. 3.9 и 6.9): интервал висит на ученике, без course_id —
-- «уехал на месяц» закрывает все его предметы и группы одним действием.
CREATE TABLE student_pauses (
    id         UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
    student_id UUID        NOT NULL REFERENCES students(id) ON DELETE CASCADE,
    starts_on  DATE        NOT NULL,
    ends_on    DATE        NOT NULL,
    reason     TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CHECK (ends_on >= starts_on)
);

CREATE INDEX idx_pauses_student ON student_pauses(student_id, starts_on);

-- +goose Down
DROP TABLE IF EXISTS student_pauses;
ALTER TABLE course_enrollments DROP COLUMN enrolled_at;
DROP INDEX IF EXISTS idx_payments_student;
ALTER TABLE payments DROP COLUMN student_id;
```

Границы паузы — `DATE`, а `lessons.scheduled_at` — `TIMESTAMPTZ` (миграция 003), поэтому сравнение приводит урок к дате в часовом поясе сессии, то есть в UTC. Для Алматы (+05) это расходится с местной датой только у занятий между полуночью и пятью утра, которых у репетитора не бывает. Часового пояса у тьютора в схеме нет — он есть только у правила повторения (`recurrence_rules.tz`), — так что городить приведение сейчас не на чем: пометить запрос `ponytail:`-комментарием с ceiling'ом «граничный день считается по UTC» и апгрейдом «когда у тьютора появится tz».

**Перед накаткой** посчитать, сколько строк останется без адресата:

```sql
SELECT count(*) FILTER (WHERE c.student_id IS NULL) AS group_payments,
       count(*)                                     AS total
  FROM payments p JOIN courses c ON c.id = p.course_id;
```

Если групповых платежей единицы — проще попросить тьютора переназначить их руками через UI после деплоя, чем писать эвристику. **Их не больше десяти** — подтверждено фаундером 2026-09-10, так что разметка руками решена: эвристика не нужна, а уточняющий шаг ниже прогоняется сразу после неё.

Вторым запросом посмотреть, скольким участникам групп бэкфилл найдёт дату по посещаемости, а скольким достанется `started_at` — это и есть список тех, у кого дата записи может оказаться слишком ранней:

```sql
SELECT count(*) FILTER (WHERE att.first_seen IS NOT NULL) AS by_attendance,
       count(*) FILTER (WHERE att.first_seen IS NULL)     AS by_course_start,
       count(*)                                           AS total
  FROM course_enrollments ce
  LEFT JOIN LATERAL (
       SELECT min(l.scheduled_at) AS first_seen
         FROM lesson_attendances la
         JOIN lessons l ON l.id = la.lesson_id
        WHERE l.course_id = ce.course_id AND la.student_id = ce.student_id) att ON TRUE;
```

**Уточняющий шаг — после ручной разметки платежей.** Когда тьютор проставит адресатов легаси-платежам группы, у части участников появится след точнее посещаемости. Прогнать один раз, руками, не в миграции:

```sql
UPDATE course_enrollments ce
   SET enrolled_at = f.first_paid
  FROM courses c,
       LATERAL (SELECT min(p.paid_at) AS first_paid
                  FROM payments p
                 WHERE p.course_id = ce.course_id
                   AND p.student_id = ce.student_id) f
 WHERE c.id = ce.course_id
   AND ce.enrolled_at = c.started_at      -- только те, кому дата досталась по умолчанию
   AND f.first_paid   > c.started_at;     -- и только если платёж был позже старта группы
```

Условие `enrolled_at = c.started_at` важно: оно не даёт перетереть даты, найденные по посещаемости, которые точнее платежа — человек ходит с сентября, а заплатил в октябре.

### 6.2. Модель и валидация

`models/payment.go`:

```go
type Payment struct {
	ID           string    `json:"id"`
	CourseID     string    `json:"course_id"`
	StudentID    *string   `json:"student_id"`     // NULL — легаси-платёж группы
	Amount       float64   `json:"amount"`
	LessonsCount int       `json:"lessons_count"`
	PaidAt       time.Time `json:"paid_at"`
	Subject      string    `json:"subject,omitempty"`
	StudentName  *string   `json:"student_name,omitempty"`
}

type CreatePaymentRequest struct {
	CourseID     string    `json:"course_id"     validate:"required,uuid"`
	StudentID    string    `json:"student_id"    validate:"required,uuid"`
	Amount       float64   `json:"amount"        validate:"required,gt=0"`
	LessonsCount int       `json:"lessons_count" validate:"required,gt=0"`
	PaidAt       time.Time `json:"paid_at"       validate:"required"`
}
```

`UpdatePaymentRequest` получает `StudentID` тем же способом — иначе легаси-платёж группы нечем починить из UI.

### 6.3. Проверка связи ученика с курсом

`paymentService.Create` сейчас проверяет только владение курсом. Добавляется проверка, что ученик действительно на этом курсе, — иначе платёж повиснет в балансе, которого никто не увидит:

```go
// Индивидуальный курс — ученик обязан совпасть с course.student_id.
// Групповой — обязан быть в course_enrollments. Иначе платёж уедет в баланс,
// который нигде не показывается.
```

Реализация — новый метод `enrollmentRepo.IsEnrolled(ctx, courseID, studentID) (bool, error)` плюс сравнение с `course.StudentID`. `paymentService` получает `enrollmentRepo` в конструктор. Точный список того, что сломается по компиляции и обязано ехать одним коммитом:

- `router/router.go:55` — `NewPaymentService(paymentRepo, courseRepo)` получает третий аргумент; `enrollmentRepo` уже создан строкой 37, переупорядочивать проводку не надо;
- `service/enrollment_test.go` — `mockEnrollmentRepo` получает `IsEnrolled`;
- `service/payment_test.go` — хелпер `newPaymentSvc` получает третий аргумент, `GetBalance` в моках — параметр `studentID`;
- `handlers/mocks_test.go` — `mockPaymentService.GetBalance` меняет сигнатуру, иначе перестаёт удовлетворять интерфейсу `PaymentService`.

Готового `IsEnrolled` нет; ближайший образец `EXISTS`-проверки принадлежности — `repository/student.go:154`.

**Проверка на существование связи, а не на её активность.** `IsEnrolled` обязан возвращать `true` и для строки с заполненным `left_at`: ушедший из группы ученик — первый, кому нужно записать погашение долга, и запрет платежа сделал бы этот долг невзыскиваемым. Условие в SQL — только `course_id` и `student_id`, без `left_at IS NULL`; это тот случай, когда «более строгая» проверка ломает сценарий, ради которого мягкое удаление и заводилось.

**Критерий истории ученика (п. 5a.1) дополняется адресными платежами** — в том же коммите, что миграция: `AND NOT EXISTS (SELECT 1 FROM payments p WHERE p.student_id = s.id)` в запросе удаления `repository/student.go:Delete`. Без этого участник группы, заплативший, но ни разу не отмеченный, удалится, и `ON DELETE CASCADE` новой колонки унесёт его платёж. С дополнением каскад по `payments.student_id` не срабатывает никогда, кроме каскада от `tutors`, — поэтому `CASCADE`, а не `SET NULL`, здесь безопасен.

### 6.4. Баланс по ученику

`models.CourseBalance` остаётся по форме, но считается в разрезе пары «курс + ученик»:

```go
GetBalance(ctx context.Context, courseID, studentID string) (models.CourseBalance, error)
```

```sql
SELECT
  COALESCE((SELECT SUM(lessons_count) FROM payments
             WHERE course_id = $1 AND student_id = $2), 0)                     AS paid,
  (SELECT count(*) FROM lessons l
     LEFT JOIN course_enrollments ce
            ON ce.course_id = l.course_id AND ce.student_id = $2
    WHERE l.course_id = $1
      AND l.status IN ('completed', 'missed')
      AND l.scheduled_at >= COALESCE(ce.enrolled_at, '-infinity'::timestamptz)
      AND l.scheduled_at <  COALESCE(ce.left_at,     'infinity'::timestamptz)
      AND NOT EXISTS (SELECT 1 FROM student_pauses sp
                       WHERE sp.student_id = $2
                         AND l.scheduled_at::date BETWEEN sp.starts_on AND sp.ends_on)) AS burned
```

Три условия — три ответа на один вопрос из п. 3.9: с какого числа уроки его, по какое и какие куски вырезаны из середины. Посещаемости здесь нет намеренно (п. 3.6). Оба `COALESCE` покрывают индивидуальный курс: строки в `course_enrollments` у него нет, `LEFT JOIN` даёт `NULL`, и в счёт идут все уроки курса — а паузы работают и там, потому что висят на ученике, а не на записи в группу.

Выражение `burned` вынести в константу рядом с `studentNameExpr` в `repository/payment.go`: оно нужно балансу, списку долгов, прогнозу и позиции в цикле — четырём запросам, которые обязаны считать одинаково. Продублированное четыре раза, оно разъедется на первой же правке.

Роут `/payments/balance?course_id=` получает обязательный `student_id`.

### 6.5. Долги — новый эндпоинт

`GET /payments/debts` → список должников тьютора, по убыванию долга:

```go
type StudentDebt struct {
	StudentID       string     `json:"student_id"`
	StudentName     string     `json:"student_name"`
	LessonsOwed     int        `json:"lessons_owed"`      // сгорело сверх оплаченного
	AmountOwed      float64    `json:"amount_owed"`       // уроки × цена урока курса (пакет / N, до тенге)
	NextLessonAt    *time.Time `json:"next_lesson_at"`    // когда напомнить
	Courses         []DebtByCourse `json:"courses"`       // разбивка: предмет + сколько должен
}
```

Считается одним запросом по всем курсам тьютора, **включая архивные**: для каждой пары «курс + ученик» — `burned − paid`, положительные остатки суммируются по ученику, число уроков умножается на цену урока своего курса — `price_per_cycle / lessons_per_cycle`, округлённую до тенге. Пара берётся из `courses.student_id` для индивидуальных и из `course_enrollments` для групп — тот же `UNION`, что уже есть в `courseRepository.GetByStudent:155-166`, только с `enrolled_at` и `left_at` в выборке. Этот `UNION` сейчас фильтрует `c.is_active = TRUE` и `ce.left_at IS NULL` — ни один из фильтров запросу долгов копировать нельзя: ушедшие из группы и архивные студенты в долги входят (п. 5a.3–5a.4).

**У пакетных тьюторов денежная сумма долга приблизительна.** 2 урока из пакета 85 000/12 — это 14 167 ₸, число, которого тьютор не назначал: он продаёт пакет, а не урок. Долг в уроках при этом точен при любом способе. Показывать таким тьюторам «к оплате следующий пакет» вместо суммы — возможное улучшение экрана «Долги», не в этой спеке.

**Ушедшие из группы из списка не выпадают.** Строки с заполненным `left_at` участвуют в расчёте наравне с остальными: долг не исчезает оттого, что человек перестал ходить, — он как раз тогда и становится единственным, что от отношений осталось. Расти он при этом не может: `burned` обрезан по `left_at`. Когда долг погашен, ученик уходит из списка сам, потому что остаток перестаёт быть положительным. По той же причине запрос не фильтрует ни `courses.is_active`, ни `students.active`: архивация курса или ученика (п. 5a.3) закрывает расписание, но не прощает долг.

Экран `/payments` получает вкладку «Долги» рядом с историей платежей. Ученик в списке кликается в свою карточку. **Кнопку «Напомнить» в этой фазе не делаем** — канал доставки не выбран (см. п. 8).

### 6.6. Прогноз с учётом состава группы

`GetMonthlyExpected` перестаёт группировать по курсу и группирует по паре «курс + ученик»: `paid_through` считается по платежам этого ученика, ранги уроков — по урокам курса начиная с `enrolled_at` этого ученика. Для группы из пяти человек прогноз становится впятеро больше — и наконец правильным (п. 1.4).

Ранги считаются тем же выражением, что и `burned` (п. 6.4), поэтому ушедшие (`left_at`) и замороженные в прогноз не попадают автоматически: уехавший на месяц ученик перестаёт быть ожидаемым доходом этого месяца — что и есть правильный ответ, ради которого заморозка заводилась.

Докблок над функцией обновить: нынешний `ponytail` про «будущие циклы по плановому `lessons_per_cycle`» остаётся в силе, к нему добавляется «ранги — по периодам участия ученика».

> **Предупредить пользователей в релизных заметках.** Прогноз дохода для тьюторов с группами вырастет кратно составу. Без объяснения это выглядит как новый баг, а не как починка старого.

### 6.7. Позиция в цикле — по ученику

`computeCyclePositions` (`service/lesson.go:575`) и `enrichLessons` получают `studentID`: ранги считаются по тем же периодам участия, что и `burned` (п. 6.4) — от `enrolled_at`, до `left_at`, мимо пауз, — платежи фильтруются по тому же ученику. Считать здесь иначе, чем в балансе, нельзя: бейдж «3 из 8» и строка «оплачено 3 из 8» на одном экране обязаны показывать одно число. Для группового курса в календаре и на странице курса цикл **не считается** (п. 3.8): `CalendarLesson.CyclePosition`/`CycleSize` остаются пустыми, если `is_group`. В карточке ученика — считаются всегда.

**Осторожно с пересечением:** эти же три выборки (`GetByCourse`, `GetByCoursePaged`, `GetByPeriod` в `repository/lesson.go`) правит спека по багам серий — она добавляет туда `rule_id` и `occurrence_date`. Если та спека уже сделана, поля на месте: не удаляй их и не переписывай запрос целиком.

### 6.8. Одна оплата на несколько предметов

Закрывает третий пункт п. 1.4. **Схема не меняется**: платёж остаётся привязан к курсу, потому что «8 занятий» имеет смысл только для конкретного предмета по конкретной цене. Меняется то, что разбивку делает форма, а не тьютор в уме.

**`POST /payments/bulk`** — тело `{ paid_at, items: [{ course_id, student_id, amount, lessons_count }] }`, все строки в одной транзакции.

Транзакция здесь обязательна, в отличие от саги в `service/onboarding.go`: два последовательных `POST /payments` могут записать половину денег, и тьютор увидит один платёж вместо двух, не поняв почему. Это деньги — «либо всё, либо ничего» дешевле компенсации. Валидация каждой строки та же, что в `paymentService.Create`, включая проверку связи ученика с курсом (п. 6.3).

**Форма «Оплата» на карточке ученика.** Показывается с разбивкой **только когда у ученика два и более активных курса**; при одном — нынешняя простая форма. Большинство учеников на одном предмете, и облагать их налогом ради редкого случая не надо.

```
Оплата — Айгерим Серикова

К распределению     [ 60 000 ] ₸        14 сентября

  Математика   40 000 ₸ за 8 ур.   долг 2 урока   [ 8 ] уроков = 40 000 ₸
  Физика        6 000 ₸ / урок     —              [ 4 ] уроков = 24 000 ₸

  Записывается 64 000 из 60 000 · разница 4 000 ₸
                                      [Отмена]  [Записать]
```

**Истина — строки, верхнее поле только считает.** Решение фаундера, 2026-09-09. В БД уезжают `items` и ничего кроме них: доход месяца складывается из строк, и введённые сверху 60 000 нигде не хранятся. Поэтому поле называется «К распределению», а не «Получено» — иначе тьютор вправе ожидать, что именно эта сумма попала в отчёт. Второй вариант — завести чек (`receipt_id` плюс фактическая сумма прихода) и показывать расхождение как отдельную сущность — отклонён: это ещё одна таблица и ещё одно число, которое надо где-то сверять, ради случая «распределил не всё».

- Вводятся **уроки**, суммы по строкам считаются и правятся руками — то же правило, что в п. 4.4: уроки × цена урока в целых тенге, за полный пакет — ровно его сумма.
- Рядом с предметом — текущий долг и кнопка «погасить долг», если он есть. Предзаполнять по-умному не надо: в деньгах предсказуемость дороже сэкономленного клика, поля стартуют с нулей. Это не противоречит п. 4.4: там предзаполняется единственное поле единственного курса, здесь предзаполнение означало бы разложить чужие деньги по предметам за тьютора.
- Строка «записывается X из Y» **информирует, но не блокирует**. Реальность грязная: округлили, простили тысячу, доплатят потом. Форма, не дающая сохранить до схождения в тенге, будет обходиться мусором в полях.

Необязательный штрих: nullable `payments.receipt_id UUID`, общий на строки одного сабмита, чтобы история читалась одной строкой «64 000 ₸ · Математика + Физика». Это только группировка — **никакой суммы чека колонка не несёт**, иначе вернётся отклонённый выше второй источник правды о деньгах. Одна колонка, никакой логики; без неё ничего не ломается — делать, только если история платежей начнёт выглядеть рвано.

### 6.9. Заморозка ученика

Уважительная причина — уехал, заболел, сессия — сегодня выражается только отменой уроков по одному, и деньги при этом всё равно сгорают у всех, кто в группе. Заводится действие «заморозить с даты по дату» на карточке ученика.

**Данные — `student_pauses` из п. 6.1**, интервал на ученике (п. 3.9). В `burned` пауза уже учтена (п. 6.4), поэтому денежная часть заморозки не требует ни строчки нового SQL: урок, попавший в интервал, не сгорает ни на индивидуальном курсе, ни в группе.

**Расписание — только для индивидуальных курсов.** При заморозке `[A, B]` сервис делает две вещи:

1. `UPDATE lessons SET status = 'cancelled'` по урокам ученика в интервале со статусом `scheduled`. Без этого фоновая джоба автозавершения из `main.go` закроет их как `completed`, и в календаре останутся «проведённые» занятия, которых не было. Групповые уроки не трогаются: они идут для остальных.
2. Сдвиг хвоста правила повторения — на длину паузы. Это отдельное действие, а не следствие первого: `Occurrences` (`service/recurrence.go:263-300`) считает вхождения от `starts_on` по календарю, а `max_count` — по датам, а не по состоявшимся занятиям, поэтому отмена уроков внутри паузы сама по себе хвост не удлиняет.

| Чем ограничено правило | Что делать при паузе длиной D, отменившей N уроков |
|---|---|
| `ends_on` задан | `ends_on += D` — конец курса съезжает на длину паузы |
| `max_count` задан | `max_count += N` — добираем ровно недостающие занятия |
| оба `NULL` (бессрочное) | ничего: хвост и так бесконечен, сдвигать нечего |

**Метода для этого сейчас нет — это незаявленный объём работы.** В `repository/recurrence.go` есть только `SetEndsOn` (строка 118), и он ставит абсолютную дату, а `max_count` не правится вообще ниоткуда: колонка читается в `Create`/`GetByID` и участвует в `Occurrences`, но `UPDATE` по ней в репозитории отсутствует. Нужен один новый метод — `ShiftTail(ctx, ruleID string, byDays int, extraCount int)`, делающий оба инкремента одним запросом (`ends_on = ends_on + $2`, `max_count = max_count + $3`, каждый — только если колонка не `NULL`). Отдельными сеттерами это делать не стоит: тогда правило на секунду окажется в состоянии «конец сдвинут, счётчик нет», и параллельная материализация успеет создать лишние вхождения.

Новых уроков руками создавать не надо: горизонт дотянет фоновая `ExtendAll` (её точка входа — `RecurrenceExtendWorker` в `worker/`), а отменённые в паузе не воскреснут — уникальный индекс `(rule_id, occurrence_date)` из миграции 033 держит их тумбстоунами, потому что `Cancel` не обнуляет `rule_id`/`occurrence_date`. Это тот случай, когда свойство, заведённое ради идемпотентности материализации, бесплатно работает второй защитой.

**Группу не сдвигаем.** Расписание группы общее, двигать его ради одного участника нельзя; у него пауза выражается только тем, что уроки интервала не сгорают. Написано явно, потому что из формулировки «отменять и сдвигать» реализатор попробует подвинуть и правило группы.

**Порядок.** Правка `ends_on`/`max_count` трогает ровно ту область, которую чинит `docs/specs/2026-09-06-recurrence-series-bugs.md` (баг 2: `scope=all` сносит будущие вхождения и не пересоздаёт их). Заморозка идёт после неё — иначе первая же пауза попадёт в известный баг и будет выглядеть как собственный.

**Разморозка** отдельным действием не нужна: удаление строки `student_pauses` возвращает уроки в счёт. Сдвинутый `ends_on` при этом назад не откатывается — курс уже пересобрал расписание, и отматывать его задним числом опаснее, чем оставить лишнюю неделю в хвосте.

**Критерии приёмки**

- Ученик на двух предметах: одна форма, один сабмит, в БД две строки с общим `paid_at`; в доход месяца попадает сумма строк, а не число из поля «К распределению».
- Форма даёт сохранить при расхождении с полем «К распределению» и показывает разницу.
- Ошибка на второй строке не оставляет первую в базе.
- Ученик на одном предмете видит прежнюю форму без таблицы разбивки.
- Группа из трёх учеников: каждый платит за 4 урока, проведено 2 занятия, один отмечен `absent` на одном из них → у всех троих осталось по 2: посещаемость денег не касается (п. 3.6).
- Ученик, записанный в группу после третьего занятия, должен за 0 уроков, а не за 3.
- Отменённый урок (`cancelled`) не уменьшает баланс ни у кого.
- Ученик с двумя предметами: платёж за математику не меняет баланс физики.
- `/payments/debts` показывает ученика с двумя долгами одной строкой и разбивкой по предметам; сумма долга равна `сгорело − оплачено` × цена урока каждого курса (пакет / N, до тенге).
- Прогноз на месяц для группы из пяти человек равен пятикратной цене пакета, а не однократной.
- Платёж нельзя создать на ученика, не записанного на курс: 400.
- Легаси-платёж без `student_id` виден в истории курса и помечен как «без адресата»; в балансы не входит.
- Бэкфилл: участник группы, впервые отмеченный на занятии в ноябре, после миграции должен за уроки с ноября, а не с сентября. Участник без единой отметки получает `started_at` курса и попадает в выборку для уточняющего шага; после разметки платежей и прогона уточняющего `UPDATE` его дата съезжает на первый платёж.
- Убранный из группы ученик с долгом остаётся в `/payments/debts`; его долг не растёт от занятий, прошедших после ухода; платёж на него по-прежнему принимается — долг можно погасить.
- Ученик, замороженный на две недели, за уроки этого периода не должен: ни на индивидуальном курсе, ни в группе, где занятия шли для остальных.
- Заморозка индивидуального курса отменяет уроки периода и сдвигает `ends_on` (или увеличивает `max_count`) правила; после `ExtendAll` в хвосте появляется столько же занятий, сколько отменено.
- Заморозка задним числом на прошедшую неделю уменьшает `burned` без правки статусов уроков.
- Заморозка ученика из группы не меняет расписание группы.

---

## 7. Фаза 3 — карточка ученика как рабочее место

**Цель:** всё, ради чего репетитор открывает человека, лежит на карточке человека. Миграций нет.

### 7.1. Агрегирующий эндпоинт

Собирать карточку шестью запросами с фронта — это шесть round-trip'ов на открытие экрана, который открывают перед каждым уроком. `GET /students/:id/overview`:

```go
type StudentOverview struct {
	Student      models.Student          `json:"student"`
	Courses      []StudentCourseSummary  `json:"courses"`       // предмет, цена, баланс, ДЗ
	NextLesson   *models.CalendarLesson  `json:"next_lesson"`
	RecentLessons []models.CalendarLesson `json:"recent_lessons"` // последние 5, со статусами
	Payments     []models.Payment        `json:"payments"`       // последние 5
	TotalOwed    float64                 `json:"total_owed"`
}
```

Сервис `studentService.Overview` компонует уже существующие методы, как это делает `service/onboarding.go` и `service/calendar.go`, — узкими интерфейсами, без новых репозиториев. Балансы всех курсов ученика брать **одним батч-запросом**, а не циклом по курсам: N+1 на этом экране заметен сразу, у ученика бывает 2–3 курса, но у страницы `/students` их сотня.

### 7.2. Структура экрана

`students/[id]/page.tsx` — вкладки вместо одной колонки:

```
Айгерим Серикова                        [Начать урок] [Пригласить] [⋯]
телефон · email · следующий урок: сегодня 17:00

[ Обзор ] [ Уроки ] [ Оплаты ] [ ДЗ и материалы ]

Обзор:
  Предметы            Математика · 40 000 ₸ за 8 ур. · оплачено 3 из 8   [править]
                      Физика     · 6 000 ₸ / урок · долг 2 урока         [править]
  Ближайший урок      Сегодня 17:00 · Математика · [Войти в комнату]
  Последние уроки     5 строк со статусами
  Долг                12 000 ₸ (2 урока)
```

Цена в строке предмета — в том виде, в каком её правит п. 5.1: пакет при N > 1, цена урока при N = 1.

Все блоки — перенос существующих компонентов со страницы курса (`courses/[id]/page.tsx`: «Баланс уроков» 320, «Оплаты» 345, «Уроки» 463). Новых компонентов не пишем, выносим существующие в `components/students/`.

### 7.3. Что остаётся на странице курса

Страница курса не удаляется: у группы нет «карточки ученика», и всё групповое (состав, посещаемость, общее ДЗ, доска) живёт там. Для индивидуального курса страница остаётся доступной по прямой ссылке, но перестаёт быть местом, куда ходят.

**Критерии приёмки**

- Открытие карточки ученика — один запрос к API, не шесть.
- С карточки ученика можно: посмотреть баланс и долг, записать оплату, поставить урок, войти в комнату, поменять цену, открыть ДЗ — не уходя на другой экран.
- Ученик с курсами по двум предметам видит оба на одном экране с раздельными балансами.

---

## 8. Фаза 4 — `/courses` → `/groups`

**Цель:** убрать из навигации служебную сущность. Миграций нет, бэкенд не меняется.

Раздел «Курсы» сегодня мешает несовместимое: индивидуальные курсы (производные, пользователь их не создавал) и группы (настоящие сущности, заведённые руками). Признак того, что раздел не первичный, уже есть в коде: в `MobileBottomNav.tsx` его нет.

- `Sidebar.tsx:404`: «Курсы» → «Группы», иконка `Users`.
- `/groups` — список только групповых курсов (`student_id IS NULL`). Фильтрация на клиенте по уже имеющемуся полю; отдельный параметр API не нужен, пока курсов у тьютора десятки.
- Индивидуальные курсы из списка уходят: они видны строками на карточке ученика (фаза 3).
- `/courses` редиректит на `/groups`; `/courses/:id` остаётся живым для прямых ссылок и для перехода с карточки ученика.
- `CourseForm` теряет ветку `individual` целиком: единственный способ завести индивидуальный курс — поставить урок (что уже так и работает через `GetOrCreateIndividual`). `schemas/course.ts` теряет `type` и `superRefine`.
- `GettingStarted.tsx:24` — шаг «Создать курс» заменить на «Добавить ученика»: он и так первый в реальном сценарии.

**Критерии приёмки**

- В навигации нет слова «курс».
- Новый тьютор проходит путь «зарегистрировался → ученик → урок → оплата», ни разу не увидев экран создания курса.
- Прямая ссылка на `/courses/:id` из закладок продолжает открываться.

---

## 9. Что сознательно НЕ делаем

- **Историю цен курса.** Смена цены задним числом не пересчитывает прошлые платежи: платёж хранит свою сумму, и она — источник правды о прошлом. Версионирование цены (`price_valid_from`) — отдельная тема, нужна тогда, когда тьюторы начнут поднимать цены посреди учебного года и жаловаться на съехавший отчёт.
- **Скидки, абонементы, несколько пакетов на один курс.** Один пакет на курс. Скидка сегодня выражается ценой конкретного курса конкретного ученика — этого хватает.
- **Цену урока как канон** (`price_per_lesson`, `lessons_per_payment` — первая редакция фазы 1). Отменено 2026-09-13 (п. 3.1): такая миграция теряла пакеты, не делящиеся на N, и показывала тьютору цену урока, которую он не назначал.
- **Переключатель «за урок / за пакет» в формах цены.** Пара полей «сумма ₸ за N уроков» с подсказкой «≈ X ₸ за урок» покрывает оба способа (п. 2): тот, кто умножает, вводит 40 000 за 8, пакетный — 85 000 за 12. Добавлять, если тьюторам с умножением станет неудобно считать сумму в уме.
- **Правило сгорания при поздней отмене** (за N часов). Требует «когда отменили» в схеме и политики отмены в продукте; см. п. 3.6.
- **Списание по посещаемости.** Рассмотрено и отклонено (п. 3.6): деньги считаются по факту состоявшегося занятия, `lesson_attendances` остаётся статистикой. Простить пропуск можно отменой урока.
- **Напоминания о долге.** Экран «Долги» показывает, кому написать; сам канал (WhatsApp, пуш, письмо) — отдельная работа, зависящая от выбора канала. Кнопка «Напомнить» появится вместе с ним.
- **Денежный баланс вместо пакетного** (`payments.course_id` nullable, платёж без курса гасит долг ученика по всем предметам, FIFO). Рассмотрено и отклонено 2026-09-08. Это не «дорогой вариант п. 6.8», а другая модель учёта, и платить за неё пришлось бы тремя вещами: предоплата перестаёт фиксировать цену («оплатил 8 занятий» больше не защищает от подорожания — сильный аргумент тьютора в разговоре с родителем); позиция в цикле лишается основы, потому что `computeCyclePositions` идёт по целым границам `lessons_count`; баланс будущих уроков становится приблизительным (по какой цене считать непроведённые). Плюс FIFO — это невидимая аллокация: пока она права, её никто не замечает, а когда ошибётся, тьютор увидит неверный баланс и не поймёт, откуда он. Продукт, который только что чинил доверие к цифрам, не должен вводить такую автоматику. Если когда-нибудь переходить — то отдельным осознанным решением, а не побочным эффектом удобной формы.
- **Дробные `lessons_count`.** Отклонено там же: у урока с дробной пачкой позиция в цикле не определена (а не «плохо считается»), плюс миграция типа колонки на денежной таблице ради случая, которого в репетиторстве почти не бывает — за половину занятия не платят.
- **Заморозка по паре «ученик + предмет».** Пауза висит на ученике целиком (п. 3.9): «уехал» и «болеет» — состояния человека, а не предмета. Случай «морожу физику, хожу на математику» существует, но платить за него полем выбора предмета в форме пришлось бы всем остальным. Колонка `course_id` в `student_pauses` добавляется одним `ALTER TABLE`, когда случай окажется частым.
- **Чек с фактической суммой прихода.** Отклонено 2026-09-09 вместе с решением по п. 6.8: истина — строки платежей, а вторая сумма рядом с ними означала бы второй источник правды о деньгах и расхождение, которое надо где-то сверять и показывать.
- **Откат сдвига при разморозке** (п. 6.9). Удаление паузы возвращает уроки в счёт, но `ends_on` правила остаётся сдвинутым: расписание уже пересобрано, и отматывать его задним числом опаснее лишней недели в хвосте.
- **Мультивалютность.** Тенге, `numeric(10,2)`.
- **Разделение «кто платит» и «кто учится»** (родитель vs ребёнок). Платёж адресуется ученику; родитель — это `students.notes` и телефон. Отдельная сущность плательщика оправдана, когда появятся семьи с несколькими детьми у одного тьютора, — тогда и заводить.
- **Пересчёт легаси-платежей группы.** Адресата им выдаёт тьютор руками через UI; эвристика «поделить поровну» создала бы уверенно неправильные балансы вместо честно пустых.
- **Физическое удаление ученика с историей.** Удаляется только ученик без платежей, проведённых уроков и отметок (п. 5a.1), остальных тьютор архивирует. Стереть человека целиком (например, по требованию об удалении персональных данных) — отдельное действие с явным предупреждением о потере денег, а не кнопка «Удалить»; заводить, когда такой запрос придёт.
- **Несколько периодов участия в одной группе.** Повторная запись снимает `left_at` и склеивает периоды (п. 5a.4). Таблица периодов нужна, когда «ушёл и вернулся в ту же группу» станет частым; до тех пор промежуток закрывается заморозкой (п. 6.9).

---

## 10. Порядок работ

| Фаза | Содержание | Миграции | Оценка | Риск |
|---|---|---|---|---|
| 0 | Единицы цены: модалка, инлайн-правка, `gte=0`, направление ввода в форме оплаты | нет | день | низкий, откат revert'ом |
| 1 | Формы цены под пакетную модель: инлайн-правка суммы пакета, пара полей в модалке, `GetOrCreateIndividual` с дефолтом одной парой | нет | полдня | низкий: схема и API не меняются |
| 1.5 | Удаление ученика только без истории, архив ученика, мягкий уход из группы (`left_at`) | 038 | 2 дня | низкий: одна nullable-колонка; меняется смысл `DELETE /students/:id` и `DELETE /courses/:id/enrollments/:studentId` — нужны интеграционные тесты |
| 2 | `payments.student_id`, периоды участия (`enrolled_at`, `student_pauses`), баланс и прогноз по ученику, экран «Долги», `POST /payments/bulk`, заморозка | 039 | 6–7 дней | средний: меняется семантика баланса, прогноз по группам вырастет кратно, заморозка трогает правила повторений — нужны интеграционные тесты |
| 3 | Карточка ученика как рабочее место, `GET /students/:id/overview` | нет | 2–3 дня | низкий |
| 4 | `/courses` → `/groups`, `CourseForm` без индивидуальной ветки | нет | 1 день | низкий |

Фазы 0 и 1 чинят деньги и идут подряд: фаза 1 закрывает порчу пакетных цен, которую внесла фаза 0 (п. 4.2), и склейку чужих пакетов в дефолтах календаря (п. 3.3). Фаза 1.5 идёт между ними: без неё удаление ученика продолжает стирать платежи, а уход из группы — записи, на которых фаза 2 строит разметку легаси-платежей. Фаза 2 даёт продукту ответ на главный вопрос пользователя и является самой ценной из оставшихся. Фазы 3 и 4 — следствие: после того как деньги переехали на человека, экран человека обязан стать основным.

## 11. Чеклист для каждой фазы

1. Миграция, если она есть в фазе, накатана и откатана на локальной БД (`make migrate-up`, `make migrate-down`, снова `up`).
2. `go build ./...` — компилируется.
3. **Все моки синхронизированы с интерфейсами** (`handlers/mocks_test.go`, `service/*_test.go`): расширение интерфейса репозитория без обновления мока ломает компиляцию тестов — главный footgun проекта.
4. `make test` зелёный.
5. `make test-integration` с поднятым `TEST_DB_URL` — обязательно для фаз 1 и 2: обе меняют SQL, который юнит-тестами с моками не проверяется.
6. `cd frontend && npm run lint`.
7. Ручной smoke по критериям приёмки фазы.
8. Прод-проверочные запросы из фазы выполнены на копии прод-данных **до** накатки.
